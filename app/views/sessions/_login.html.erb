
<div class="container">
	<div class="flash_danger">
		<%= flash.now[:danger] if flash.now[:danger] != nil %>
	</div>
	<div class="flash_success">
		<%= flash.now[:success] if flash.now[:success] != nil %>
	</div>
	<br>
	<%= form_for(:session, url: login_path, layout: :basic)do |f| %>
	<%= f.email_field :email %>
	<%= f.password_field :password %>
	<%= f.check_box :remember_me, checked: true %>
	<%= f.submit 'Sign in' %>
	<% end %>
	<%= link_to 'New User? Click here to sign up!', new_user_path %>
</div>


<style>
.flash_danger{
	background-color: #f2dede;
	border-color: #eed3d7;
	color: #b94a48;
	text-align: left;
	font-weight: bold;
}

.flash_success{
	border-color: #eed3d7;
	text-align: left;
	font-weight: bold;
}
</style>
