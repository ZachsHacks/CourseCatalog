<!-- <% if session[:user]!=nil && !$redis.zscore(session[:user], @public_user.id).nil? %>
	<button id="user" type="submit" class="btn btn-primary btn-xs" name="unfollow" value="<%= @public_user.id %>">
      <span class="glyphicon glyphicon-user" aria-hidden="true"></span>Unfollow
    </button>
<% elsif session[:user]!=nil %> -->
  <button id="user" type="submit" class="btn btn-primary btn-xs" name="follow" value="<%= @public_user.id %>">
 <span class="glyphicon glyphicon-user" aria-hidden="true"></span>Enroll
    </button>
<!-- <% end %> -->
